function area (length, width) {
    var areas = (length*width);
    console.log(areas);
    var perimeter = (2 * length + 2 * width);
    console.log(perimeter);
}

area(40,50);
area(30,40);
area(600,500);